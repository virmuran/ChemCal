# ChemCal 依赖列表
# 安装：pip install -r requirements.txt

# GUI 框架
PySide6>=6.5.0

# 科学计算
numpy>=1.24.0
scipy>=1.10.0

# PDF 生成（计算书导出，reportlab 为旧方案，逐步废弃）
reportlab>=4.0.0

# PDF 生成（纯物质物性、腐蚀查询等计算器的 PDF 导出）
fpdf2>=2.8.0

# DOCX 报告生成（新方案，替换 TXT 导出）
python-docx>=0.8.11

# DOCX → PDF 转换（需要 Microsoft Word，可选）
pywin32>=305

# 系统监控（帮助 > 系统信息）
psutil>=5.9.0

# 日志记录
loguru>=0.7.0
