# ChemCal 版本号规范

> 本文是 ChemCal 版本号的**唯一权威标准**。写代码时只允许手改 `version.py`，
> 其余位置（`ChemCal.iss`、`README` 徽章与更新日志）由
> `bump_version.py` 自动同步，**不要手改**。

## 一、格式

```
主版本 . 次版本 . 修订号
  1   .   6   .   0
```

- **严格三段**，纯数字，段内**禁止前导零**（写 `1.6.1`，不写 `1.6.01`）
- 发布时 tag 固定为 `v` + 版本号，如 `v1.6.0`
- 不带日期、不带 `-beta`/`-final` 之类后缀（预发布例外，见第七节）

| 段位 | 位数 | 谁需要在意 |
|---|---|---|
| 主版本 | 1~2 位 | 用户：升级后要不要备份 / 重配 |
| 次版本 | 1~2 位 | 用户：有没有新东西 |
| 修订号 | 1~3 位 | 用户：无感，闭眼升 |

### 位数是自然进位的，不需要你"控制"

`1.6.9` 的下一个就是 **`1.6.10`**，跟十进制一样自然进位，可以一路走到 `1.6.99` → `1.6.100`。

- **永远不要补零**：写 `1.6.10`，**不能**写 `1.6.010`。补零的坏处是实打实的 ——
  `1.6.09` 和 `1.6.9` 解析出来都是 `(1,6,9)`，**两个不同的号被当成同一个版本**，tag 会互相打架
- 表里的"位数上限"（主/次 ≤ 2 位、修订 ≤ 3 位）**不是让你手工对齐宽度**，
  而是一张**防错网**：修订号写到 4 位以上，基本只有两种可能 —— 把日期当版本号
  （`1.4.20260623`），或者四段丢了点
- 真撞到上限（`1.6.1000`）时**不要想着放宽规则**，而要回头想：是不是该升次版本了？
  同一个次版本下攒了 999 个修订，本身就说明这个次版本线早该收尾
- 顺带一提：资源管理器按**名称排序**时 `1.6.10` 会排在 `1.6.9` 前面（字符串排序），
  别靠文件名排序判断新旧；更新器是**按数值**比较的，不受影响

## 二、什么时候升哪一位

| 升哪位 | 触发条件 | 典型改动 | 用户预期 |
|---|---|---|---|
| **主版本** MAJOR | **不兼容**变更：老版本的数据/配置在新版本里不能直接用 | 计算历史数据库结构破坏性调整、配置目录搬家、GUI 框架更换（PySide6→别的）、报告模板格式不兼容 | "要备份、可能要重新配置" |
| **次版本** MINOR | **新增功能**，向后兼容 | 新增或恢复计算器、新增标签页/模块、新增换算类别、新增参考资料类目、新增发行方式 | "多了新东西，原来的照用" |
| **修订号** PATCH | **修**：不改接口、不加功能 | 修 bug、公式勘误、单位修正、默认值调整、文案、依赖升级、打包配置、性能优化 | "更稳了，无脑升" |

### 判断口诀（一句话）

0. **先问一句：当前这个号发出去没有？** 没发出去（只在 git 里）就**不用升号**，
   新改动直接并入当前号一起发 —— 详见第七节"窗口期"
1. 升级后老用户的东西**会不会不见或要重配**？会 → **主版本**
2. 用户**能不能看见新东西**？能 → **次版本**；不能 → **修订号**
3. 只有一个动词：**加** → 次版本；**修** → 修订号
4. **拿不准就用修订号**（下次补得回来；虚高就没法往回收）

> 一次发布里既有新功能又有 bug 修复 → 按**最高档**算，即升次版本；
> 那一堆 bug 修复写进同一条更新日志即可。

## 三、硬性规则（十条）

1. `version.py` 是**唯一**手写版本号的地方，其余文件由脚本同步
2. Release 的 **tag 必须与 `version.py` 完全一致** —— 自动更新只读 tag，资产文件名不参与比较（`tag=v1.5.50` + 资产 `1.5.51` 的老事故就是栽在这）
3. tag 固定 `vX.Y.Z`，**不要用 `update` 这类自由文本 tag**：解析不出数字会退化成 `0`，一旦它成为"最新发布"，所有用户永远显示已是最新版本
4. 版本号**只增不减**；已发布过的号**永不复用**，即使撤包也要发更高的号
5. **一个版本号只发布一次**：同一个号不能用两份不同内容发两次（否则无法判断用户装的是哪一版）
6. **修订号每次只 +1**，禁止跳号（`1.5.4` 后面是 `1.5.5`，不是 `1.5.41`）
7. **禁止"四段丢点"**：本意 `1.5.4.1` 却写成 `1.5.41`，会被解析成"修订 41"，直接造成版本比较倒挂（见第五节）
8. **日期不进版本号**：`1.4.20260623` 这类看似有序，其实含义不明且与比较规则打架
9. 主版本 / 次版本数字**各自独立递增**：升次版本时修订号归零，升主版本时后两段都归零
10. 升号一律走 `bump_version.py`，`build_release.py` 会在打包前做闸门校验，不合规直接中断

## 四、为什么要有规范：三个真实事故

### 事故 1 — 版本比较倒挂（最严重）

历史提交顺序是 `1.5.2 → 1.5.21 → 1.5.22 → 1.5.23 → 1.5.3 → 1.5.4`。
当时的本意是"1.5.2 的第 1/2/3 次修订"（即四段的 `1.5.2.1`），写的时候丢了点。
而更新器是按**段位数值**比较的：

```
1.5.23  ->  (1, 5, 23)
1.5.3   ->  (1, 5,  3)     23 > 3  ⇒  1.5.23 被判定为比 1.5.3 更新
```

结果是：装在 `1.5.3` 上的用户如果遇到 `1.5.23` 成为最新发布，客户端会弹"发现新版本"
并引导**降级**；反过来 `1.5.23` 的用户则收不到 `1.5.3`。

### 事故 2 — tag 与资产版本不一致

某个 Release 的 tag 写 `v1.5.50`，上传的资产却是 `ChemCal_1.5.51_setup.exe`。
客户端只认 tag，于是所有老用户点"检查更新"都得到"已是最新版本"，更新通道静默失效。

### 事故 3 — 同一个号发多次

`v1.5.0` 有 4 次提交（其中一次还写成大写 `V1.5.0`）。出问题时无法回答
"用户装的到底是哪一个 1.5.0"，也无法判断该不该提示更新。

## 五、历史版本号复盘

| 实际用过 | 性质 | 判定 |
|---|---|---|
| `v1.0` `v1.1` `v1.2` `v1.3` | 主.次 | ✅ 规范 |
| `v1.4.20260602` `v1.4.20260623` `v1.4.20260624` `v1.4.20260625` | 日期当修订号 | ❌ 违反规则 8 |
| `v1.5.0`（4 次提交，含 `V1.5.0`） | 一号多发 + 大小写不一 | ❌ 违反规则 3、5 |
| `v1.5.21` `v1.5.22` `v1.5.23` | 四段丢点（本意 1.5.2.x） | ❌ 违反规则 7，导致倒挂 |
| `v1.5.3` `v1.5.4` | 主.次.修订 | ✅ 规范 |
| `v1.5.41` … `v1.5.46` | 四段丢点（本意 1.5.4.x） | ❌ 违反规则 7 |
| `v1.5.50` | 跳号（从 46 直跳 50） | ❌ 违反规则 6 |
| `v1.5.51` | 主.次.修订 | ✅ 规范（保留，不回收） |
| `update`（git tag） | 自由文本 tag | ❌ 违反规则 3 |

> 已经发出去的号**不追改**（改了反而会让老用户的方向判断错乱），
> 规范从下一个版本开始生效。

## 六、收敛方案：从 1.6.0 开始

- `1.5.51` **保留不动**（已发布，降级会让用户收不到更新）
- 下一个正式发布用 **`1.6.0`**：
  - 这一轮实际是"45 个计算器公式全量核对收官 + 恢复常压储罐壁厚 + 双发行打包 + 更新器重构"，
    属于**功能与发行层面**的改动，按第二节应当升**次版本**
  - 同时顺势把修订号归零，摆脱 `1.5.4x / 1.5.5x` 的历史包袱
- 之后：`1.6.1`、`1.6.2` … 修 bug 逐格递增；再有新功能 → `1.7.0`
- 哪天真做数据破坏性调整 → `2.0.0`

操作：

```bash
.venv/Scripts/python.exe bump_version.py minor "45 个计算器公式全量核对收官；恢复常压储罐壁厚；双发行打包"
```

## 七、特殊情况

| 情况 | 做法 |
|---|---|
| 预发布 / 内测包 | 用后缀：`1.6.0-rc.1`，并在 GitHub 上勾 **Pre-release**。⚠ 自动更新器**不识别** Pre-release，内测包必须手动发给测试者 |
| 线上出严重 bug 紧急修 | 走正常修订号（`1.6.1`），**不要**发明 `1.6.0-hotfix1` 这种自由格式 |
| 发出去的包有严重问题 | 发**更高**的号覆盖（如 `1.6.1` → `1.6.2`），并把有问题的 Release 下架；**不要**复用旧号 |
| 撤销发布（Release 删掉） | tag 也一并删；号码作废但**不回收**，下次仍用更高的号 |
| 新版本改了数据存储结构 | 主版本 +1，且要在更新日志里明确写出"升级前请备份 `.ChemCal/` 目录" |
| 一次攒了很多修复再发 | 仍按最高档算一次升号，一个版本号一次发布 |

### 窗口期：改动落在"已提交但还没发布"之间，要不要升号？

**不升。** 版本号是**发布**的标识，不是**提交**的标识 —— 只要这个号还没有 tag、
没有 GitHub Release、没有打包产物，它就没"发出去"过，后续提交直接**并入当前号**
一起发即可。理由：用户手里还是上一个号，他们只会经历一次升级，
`1.6.0` 这个号也只会出现一次（符合规则 5）。

判定"当前号发了没"，三处一起看，**任一为真即已发布**：

```bash
git tag -l 'v<当前号>'                 # ① 本地有 tag？
git ls-remote --tags origin            # ② 远端有 tag？（更新器读的是这个）
ls dist/ installer/                    # ③ 有 ChemCal_<当前号>_* 产物？
```

三处全为空 = 只是提交进了 main，可以放心并入。**别把"提交了"当成"发布了"** ——
代码推到 GitHub main 分支、README 徽章写上新号、commit message 写着 `v1.6.0`，
这些都不算发布；只有**打了 tag + 建了 Release 并 Publish**才是。

**唯一的例外**：这个号虽然没正式发布，但**包已经发给别人了**（内测、客户试用、
发给同事看效果）。那它就已经"流通"了 —— 两份内容不同的同号包会造成事故 3 那种
"说不清用户装的是哪一版"，此时**必须升号**：若是性能优化 / 修 bug 就升修订号
（`1.6.0` → `1.6.1`），若是加功能就升次版本。

## 八、日常工作流

```bash
# 1. 升版本号（唯一需要手做的版本相关操作，patch/minor/major 三选一）
.venv/Scripts/python.exe bump_version.py patch "修复安全阀 Kd 阀型映射"

#    想先看看会改什么：
.venv/Scripts/python.exe bump_version.py patch "说明" --dry-run

# 2. 把 README 里自动生成的日志条目写详细（脚本只放骨架）
# 3. 打包（内部含版本号闸门：格式不合法 / 没升号 / 撞 tag 都会中断）
.venv/Scripts/python.exe build_release.py

# 4. 提交推送 → 打 tag → GitHub 建 Release 并上传两个产物
git tag v1.6.0 && git push origin v1.6.0
```

发布时必须做到：
- Release 的 **tag = `version.py` 的版本号**（带 `v`）
- 上传资产命名为 `ChemCal_<版本号>_setup.exe` 与 `ChemCal_<版本号>_portable.zip`
- 点击 **Publish release**（Draft / Pre-release 更新器识别不到）
- `build_release.py` 结尾会自动做一次**远端一致性检查**，tag 与资产版本不符会直接点名

## 九、相关文件

| 文件 | 作用 |
|---|---|
| `version.py` | 版本号唯一来源，含校验函数 `is_valid_version()` |
| `bump_version.py` | 升号工具：校验 + 同步 `ChemCal.iss` / `README` |
| `build_release.py` | 一键发版，打包前执行版本号闸门 + 远端一致性检查 |
| `updater.py` | 自动更新，只读 Release 的 tag 与本地版本号比较 |
| `tests/test_versioning.py` | 版本规范的回归测试 |
