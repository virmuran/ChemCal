# ChemCal v1.6.0（2026-09-16 发布）

> 这一版两件大事：**启动快了一个数量级**，以及**倒计时、计算历史两个页面基本重做**。

---

## ⚡ 启动 7.1 秒 → 0.3 秒；切换主题 5.5 秒 → 0.4 秒

以前打开软件要等七八秒，切一次主题要等五六秒，老电脑上更明显。

原因是软件在启动时就把 45 个计算器和 21 个换算页**全部**造了出来（全窗口 5293 个控件），而 Qt 每换一次主题都要给每个控件重算一遍样式。

这一版改成**用到哪个才建哪个**：启动只登记菜单，点到哪个计算器才当场构建（约 0.1 秒，几乎无感），建过的会留着，切回来不重建。

| 场景 | 上一版 | v1.6.0 |
|---|---|---|
| 启动 | 约 7.1 秒 | **约 0.3 秒** |
| 切换主题 | 约 5.5 秒 | **0.3 ~ 0.4 秒** |
| 首次打开某个计算器 | — | 约 0.1 秒 |

---

## 🕘 计算历史：从"看过就算"变成能查、能统计、能交差

- **时间范围筛选**：今天 / 近 7 天 / 近 30 天 / 本月 / 全部
- **多选 + 批量删除**，也可以**按当前筛选一键清空**（带二次确认）
- **使用统计**：总记录数、常用分类占比、计算器排行、按日期与月份的趋势
- **导出**：CSV（Excel 直接打开，不乱码）、Word / PDF 计算书（带项目信息表头，可直接归档）

> 时间筛选与导出用同一套筛选口径，不会出现"列表里 5 条、导出却是 6 条"。

---

## ⏱ 倒计时页重做

- 日期时间改为**日历选择器**，并加了 **+1 小时 / +1 天 / +7 天 / +30 天** 快捷按钮（排工期常用）
- 新增 / 编辑合并到一个对话框，不再连弹三个输入框
- 新增**状态筛选**（全部 / 未到期 / 已过期）与**排序**（剩余时间 / 添加时间）
- 新增**一键清理已过期**
- 卡片上有**进度条**（从创建到目标的已等待比例），一眼看出还剩多少
- 到点提醒**只响一次**，不再反复弹窗
- 顺手修掉两个老毛病：每秒整页重建卡片（拖窗口时更明显）、深色主题下卡片文字看不清

---

## 🔄 单位换算：11 类 → 21 类

新增 **流量、密度、动力粘度、运动粘度、表面张力、导热系数、传热系数、比热容、热值、浓度**。

其中**浓度**支持 `% ↔ mol/L` 跨类换算（需要填密度和摩尔质量）。

---

## 🎨 深色主题：这几处以前根本看不清

- **计算历史详情**（22 处）与**资料库内容**（8 处）之前写死了浅色底 + 深色字，切到深色主题就成了"深底压深字"——现在全部跟主题走
- **常压储罐壁厚计算器**里三个自动填入的参数框（许用应力 / 水压试验 / 钢板密度）同样是写死的浅灰底，深色下几乎看不见——已修
- 主题新增语义样式与内容配色，三套主题（浅色 / 深色 / 蓝色）同进同出

---

## 🛠 另外

- 修掉常压储罐壁厚计算器一个隐藏 bug：材质从"自定义"切回 Q345R 等牌号后，本该只读的三个参数框**没有恢复只读**，现在会自动收回
- 资料库补全 5 个分类的图标（消防安全 / 水质标准 / 热工设备 / 防爆区域 / 投资估算）

---

## 📦 下载

| 文件 | 大小 | 适合 |
|---|---|---|
| `ChemCal_1.6.0_setup.exe` | 约 50 MB | **推荐**。双击安装，有开始菜单与桌面快捷方式，以后可自动更新 |
| `ChemCal_1.6.0_portable.zip` | 约 55 MB | 免安装，解压即用。适合放 U 盘，或没有管理员权限的电脑 |

---

## ⚠️ 升级提示

- **本次没有改动数据格式**，配置（`ChemCal_data.json`）和计算历史都能直接沿用，**不需要重新配置**。习惯上仍建议先备份 `%USERPROFILE%\.ChemCal\` 目录
- 已经装过 1.5.x 的：**用 `setup.exe` 覆盖安装**即可，不要解压便携包去覆盖旧目录
- 受控电脑（公司域环境）如果安装报"错误 5：拒绝访问"，请**右键 → 以管理员身份运行**安装包

---

## 🐣 已知限制（说实话）

- 如果你在**同一次使用里**把 45 个计算器逐个都打开过，之后切换主题会明显变慢（退回几秒级）。这是 Qt 全局重算样式的固有代价，**关掉软件重开即恢复**——日常用几个换几个，永远是 0.3 秒
- 首次打开某个计算器约有 0.1 秒的构建延迟（打开过一次就没有了）
